משימה: ייצא את תסריט הפודקאסט הנוכחי לקובץ Word (.docx).

**שלב 1 — אישור שם קובץ**

שאל את המפתחת:
- מה מספר הפרק? (לדוגמה: 02)
- מה כותרת הפרק? (לדוגמה: למה-anachnu-be-loop)

שם הקובץ יהיה: `podcast-NN-title.md` ו-`podcast-NN-title.docx`

**שלב 2 — שמירת קובץ מרקדאון**

שמור את הטבלה המלאה של התסריט לקובץ:
`/home/user/Lnet/output/podcast-NN-title.md`

הקובץ צריך לכלול בראשו:
```
# פודקאסט NN: <כותרת>

**קורס:** <שם הקורס>
**מבוסס על:** <פרקים X-Y>
**זמן האזנה משוער:** כ-10 דקות
**שחקנים:** יצחק לוי / חיים מנחם

---
```

ואחריו הטבלה המלאה.

**שלב 3 — הפקת DOCX**

הרץ:
```bash
python /home/user/Lnet/output/build_docx.py /home/user/Lnet/output/podcast-NN-title.md
```

**שלב 4 — אישור**

בדוק שהקובץ נוצר ודווח:
- נתיב קובץ ה-.md
- נתיב קובץ ה-.docx
- מספר שורות בטבלה

אם הסקריפט נכשל — הצג את הודעת השגיאה וטפל בה.

---

**הערה:** הסקריפט `build_docx.py` מייצר קובץ Word עם:
- טבלה ממוספרת ב-RTL עברית
- גופן David
- 4 עמודות: # | דובר | תוכן | הערות הפקה
